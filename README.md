# Resume Site

Static résumé page for `resume.thaiv.dev` with a stable `/resume.pdf` download.

## Local development

```bash
npm install
npm run dev
```

Add the current résumé as `public/resume.pdf`. A push to `main` builds the site and calls the shared static deployment adapter.
